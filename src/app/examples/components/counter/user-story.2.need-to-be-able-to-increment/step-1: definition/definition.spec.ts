describe('Counter component', () => {
  describe('Given: initialization completed', () => {
    describe('When: rendered', () => {
      xit('Then: should display a button with the label="increment"', () => {});

      xit('Then: should display a counter that is initialized to zero', () => {});

      describe('When: the increment-button is clicked', () => {
        xit('Then: the counter should be incremented', () => {});
      });
    });
  });
});
