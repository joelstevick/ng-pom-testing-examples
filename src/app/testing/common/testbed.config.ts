import { MatProgressBarModule } from "@angular/material/progress-bar";

/**
 * This is where you should add common dependencies such as 
 * ReactiveFormsModule, MaterialInputModule, and common mocked providers 
 * as required by your use-case
 */
export const commonTestbedConfig = {
      imports: [MatProgressBarModule],
}

